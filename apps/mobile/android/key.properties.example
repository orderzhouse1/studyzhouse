# Copy to android/key.properties (never commit key.properties or .jks files).
# Paths are relative to the android/ directory unless absolute.

storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=upload
storeFile=upload-keystore.jks
